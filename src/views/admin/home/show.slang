.container
  .row
    .col-md-6.offset-md-3
      h4 System Facts
      ul
        li #{user_count} users on the system.
        li #{domain_count} domains being monitored.
        li #{route_count} routes being monitored.
